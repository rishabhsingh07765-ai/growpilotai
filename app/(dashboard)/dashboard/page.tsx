import Topbar from '../../../components/topbar';import {ArrowUpRight,Camera,Plus,Sparkles,TrendingUp} from 'lucide-react';import Link from 'next/link';import {getCurrentUser} from '../../../lib/auth';
export default async function Dashboard(){const u=await getCurrentUser();return <><Topbar title="Dashboard"/><div className="p-6 md:p-8 space-y-7"><div className="flex flex-col md:flex-row md:items-center justify-between gap-4"><div><h2 className="text-2xl font-bold">Good to see you, {u?.name?.split(' ')[0]||'Creator'}.</h2><p className="mt-1 text-sm text-slate-500">Your content engine at a glance.</p></div><Link href="/studio" className="primary"><Plus size={17}/> Create content</Link></div><div className="grid md:grid-cols-4 gap-4">{[['AI generations','0','Start creating'],['Scheduled posts','0','No posts queued'],['Published','0','Connect Camera'],['Engagement','—','Awaiting Insights']].map(([a,b,c])=><div className="card p-5" key={a}><div className="text-xs text-slate-500">{a}</div><div className="mt-3 text-2xl font-bold">{b}</div><div className="mt-1 text-xs text-slate-600">{c}</div></div>)}</div><div className="grid lg:grid-cols-3 gap-5"><section className="lg:col-span-2 card p-6"><div className="flex justify-between"><div><h3 className="font-semibold">Content performance</h3><p className="text-xs text-slate-600 mt-1">Real data appears after official Camera connection.</p></div><TrendingUp className="text-fuchsia-400" size={20}/></div><div className="mt-8 h-52 rounded-2xl border border-dashed border-white/10 flex items-center justify-center text-sm text-slate-600">Connect Camera to populate your performance graph.</div></section><section className="card p-6"><Sparkles className="text-fuchsia-400" size={20}/><h3 className="mt-4 font-semibold">AI recommendation</h3><p className="mt-3 text-sm leading-6 text-slate-500">Generate your first posts and connect your account. GrowPilot will use available performance data to create recommendations.</p><Link href="/strategy" className="mt-5 inline-flex items-center gap-2 text-sm text-fuchsia-300">Open strategy <ArrowUpRight size={15}/></Link></section></div><section className="card p-6"><div className="flex items-center gap-2 font-semibold"><Camera size={18}/> Camera</div><p className="mt-2 text-sm text-slate-500">{u?.instagramAccounts?.length?'Account connected.':'No account connected yet.'}</p><Link href="/Camera" className="mt-4 inline-flex text-sm text-fuchsia-300">Manage connection →</Link></section></div></>}
