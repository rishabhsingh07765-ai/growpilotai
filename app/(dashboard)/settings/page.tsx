'use client';import {useEffect,useState} from 'react';import Topbar from '../../../components/topbar';import {Save} from 'lucide-react';export default function Settings(){const [name,setName]=useState(''),[niche,setNiche]=useState(''),[voice,setVoice]=useState('Professional'),[language,setLanguage]=useState('English'),[message,setMessage]=useState('');useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(d=>{const b=d.user?.brandProfile;if(b){setName(b.name||'');setNiche(b.niche||'');setVoice(b.brandVoice||'Professional');setLanguage(b.language||'English')}})},[]);async function save(){const r=await fetch('/api/profile',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({name,niche,brandVoice:voice,language})});setMessage(r.ok?'Saved.':'Could not save.')}return <><Topbar title="Brand Settings"/><div className="p-6 md:p-8 max-w-3xl"><div className="card p-6 space-y-5"><div><h2 className="font-semibold">Brand profile</h2><p className="text-sm text-slate-500 mt-1">These settings guide every AI generation.</p></div><input className="field" value={name} onChange={e=>setName(e.target.value)} placeholder="Brand / creator name"/><input className="field" value={niche} onChange={e=>setNiche(e.target.value)} placeholder="Niche"/><div className="grid md:grid-cols-2 gap-4"><select className="field" value={voice} onChange={e=>setVoice(e.target.value)}><option>Professional</option><option>Friendly</option><option>Educational</option><option>Funny</option><option>Premium</option><option>Bold</option><option>Inspirational</option></select><select className="field" value={language} onChange={e=>setLanguage(e.target.value)}><option>English</option><option>Hindi</option><option>Hinglish</option><option>Marathi</option></select></div><button onClick={save} className="primary"><Save size={16}/> Save changes</button>{message&&<span className="text-sm text-emerald-400">{message}</span>}</div></div></>}
