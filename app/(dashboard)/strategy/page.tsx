import Topbar from '../../../components/topbar';import {Sparkles,Target,Lightbulb} from 'lucide-react';import {getCurrentUser} from '../../../lib/auth';export default async function Strategy(){const u=await getCurrentUser();const niche=u?.brandProfile?.niche||'your niche';return <><Topbar title="AI Strategy"/><div className="p-6 md:p-8 space-y-6"><div className="card p-7"><div className="flex gap-4"><div className="w-11 h-11 rounded-xl bg-fuchsia-500/10 flex items-center justify-center"><Sparkles className="text-fuchsia-400"/></div><div><h2 className="text-xl font-bold">Your content strategy</h2><p className="text-sm text-slate-500 mt-1">Built around <span className="text-slate-300">{niche}</span>. Add more brand information in Settings to make this more specific.</p></div></div></div><div className="grid md:grid-cols-3 gap-5">{[['Education','Teach practical ideas your audience can save and share.'],['Proof','Use examples, mini case studies and results where you have evidence.'],['Conversation','Ask useful questions and create posts that invite genuine discussion.']].map(([a,b])=><div className="card p-6" key={a}><Target className="text-cyan-400" size={20}/><h3 className="mt-4 font-semibold">{a}</h3><p className="mt-2 text-sm text-slate-500 leading-6">{b}</p></div>)}</div><div className="card p-6"><div className="flex items-center gap-2 font-semibold"><Lightbulb className="text-amber-300" size={18}/> AI learning loop</div><p className="mt-3 text-sm leading-6 text-slate-500">Once real Insights are available, this area can compare content performance, identify patterns and suggest future content pillars. It will not claim causation from weak data.</p></div></div></>}
