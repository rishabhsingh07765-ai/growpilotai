import Topbar from '../../../components/topbar';import {getCurrentUser} from '../../../lib/auth';export default async function Profile(){const u=await getCurrentUser();return <><Topbar title="Profile"/><div className="p-6 md:p-8 max-w-2xl"><div className="card p-6"><div className="w-14 h-14 rounded-full bg-fuchsia-500/10 flex items-center justify-center text-xl font-bold text-fuchsia-300">{u?.name?.[0]||'U'}</div><h2 className="mt-4 text-xl font-bold">{u?.name}</h2><p className="text-sm text-slate-500">{u?.email}</p><p className="mt-6 text-sm text-slate-500">Your account and brand profile are stored server-side.</p></div></div></>}
