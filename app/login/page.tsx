'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {Sparkles} from 'lucide-react';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const d=await x.json();setLoading(false);if(!x.ok)return setError(d.error||'Login failed');r.push('/dashboard');r.refresh()}return <main className="min-h-screen grid lg:grid-cols-2 bg-[#05070b]"><div className="hidden lg:flex p-12 flex-col justify-between bg-gradient-to-br from-fuchsia-950/50 via-[#080b12] to-cyan-950/30"><div className="flex items-center gap-2 font-bold text-xl"><Sparkles className="text-fuchsia-400"/> GrowPilot AI</div><div><h1 className="text-5xl font-bold leading-tight">Turn your Instagram into an AI-powered content engine.</h1><p className="mt-5 text-slate-400 max-w-lg">Plan, create, schedule and learn from your content in one place.</p></div><p className="text-xs text-slate-600">Secure server-side integrations. No Instagram passwords stored.</p></div><div className="flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-md space-y-5"><div><div className="lg:hidden flex items-center gap-2 font-bold text-xl mb-10"><Sparkles className="text-fuchsia-400"/> GrowPilot AI</div><h2 className="text-3xl font-bold">Welcome back</h2><p className="mt-2 text-slate-500">Sign in to your growth workspace.</p></div>{error&&<div className="rounded-xl border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-300">{error}</div>}<input className="field" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="field" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="primary w-full" disabled={loading}>{loading?'Signing in…':'Sign in'}</button><p className="text-sm text-slate-500">New here? <Link href="/signup" className="text-fuchsia-300">Create an account</Link></p></form></div></main>}
