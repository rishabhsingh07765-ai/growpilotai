import {NextResponse} from 'next/server';import {getCurrentUser} from '../../../../lib/auth';import {generateContent} from '../../../../lib/ai';import {contentSchema} from '../../../../lib/validators';
export async function POST(req:Request){const u=await getCurrentUser();if(!u)return NextResponse.json({error:'Unauthorized'},{status:401});try{const d=contentSchema.parse(await req.json());const r=await generateContent({topic:d.topic,extra:d.extra,niche:u.brandProfile?.niche||undefined,audience:JSON.stringify(u.brandProfile?.audience||''),tone:u.brandProfile?.brandVoice||undefined,language:u.brandProfile?.language||undefined,goal:JSON.stringify(u.brandProfile?.goals||'')});return NextResponse.json(r)}catch(e:any){return NextResponse.json({error:e?.message||'Generation failed'},{status:400})}}
