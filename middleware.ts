import {NextResponse} from 'next/server';import type {NextRequest} from 'next/server';
export function middleware(req:NextRequest){const protectedPath=req.nextUrl.pathname.startsWith('/dashboard')||req.nextUrl.pathname.startsWith('/studio')||req.nextUrl.pathname.startsWith('/calendar')||req.nextUrl.pathname.startsWith('/analytics')||req.nextUrl.pathname.startsWith('/settings')||req.nextUrl.pathname.startsWith('/instagram')||req.nextUrl.pathname.startsWith('/strategy')||req.nextUrl.pathname.startsWith('/billing')||req.nextUrl.pathname.startsWith('/profile');if(protectedPath&&!req.cookies.get('gp_session'))return NextResponse.redirect(new URL('/login',req.url));return NextResponse.next()}
export const config={matcher:['/dashboard/:path*','/studio/:path*','/calendar/:path*','/analytics/:path*','/settings/:path*','/instagram/:path*','/strategy/:path*','/billing/:path*','/profile/:path*']};
